# Инструкция по публикации расширения TMetrix на VS Code Marketplace

## ✅ Что уже сделано

1. ✅ **package.json** обновлен:
   - displayName: "TMetrix - Time Tracking"
   - description, keywords, categories
   - publisher: "tmetrix" (замените на ваш реальный publisher ID)
   - license: MIT
   - repository, homepage, bugs URLs (обновите на реальные)
   - Настройки расширения (API endpoint, thresholds)
   - Команды обновлены

2. ✅ **LICENSE** файл создан (MIT License)

3. ✅ **README.md** обновлен с полной документацией

4. ✅ **CHANGELOG.md** заполнен для версии 0.1.0

5. ✅ **extension.ts** обновлен:
   - Использует настройки из конфигурации
   - Новые команды: `tmetrix.showStats`, `tmetrix.resetApiKey`
   - Конфигурируемый API endpoint

6. ✅ **.vscodeignore** оптимизирован

## ⚠️ Что нужно сделать перед публикацией

### 1. Создать иконку расширения
**ОБЯЗАТЕЛЬНО!** Маркетплейс примет расширение без иконки, но это выглядит непрофессионально.

См. инструкции в файле `ICON_INSTRUCTIONS.md`.

Быстрый способ:
```bash
# Создайте файл icon.svg с содержимым из ICON_INSTRUCTIONS.md
# Затем конвертируйте в PNG:
convert -background none icon.svg -resize 128x128 icon.png
```

### 2. Обновить информацию о publisher

В `package.json` поле `"publisher": "tmetrix"` нужно заменить на ваш реальный Publisher ID из VS Code Marketplace.

**Как получить Publisher ID:**
1. Зайдите на https://marketplace.visualstudio.com/manage
2. Войдите с Microsoft/GitHub аккаунтом
3. Создайте Publisher (если еще нет)
4. Скопируйте Publisher ID
5. Замените в package.json

### 3. Обновить URLs в package.json

Замените в `package.json`:
```json
"repository": {
  "type": "git",
  "url": "https://github.com/ВАША_ОРГАНИЗАЦИЯ/ВАША_РЕПО"
},
"bugs": {
  "url": "https://github.com/ВАША_ОРГАНИЗАЦИЯ/ВАША_РЕПО/issues"
},
"homepage": "https://github.com/ВАША_ОРГАНИЗАЦИЯ/ВАША_РЕПО#readme"
```

### 4. Установить vsce (VS Code Extension Manager)

```bash
npm install -g @vscode/vsce
```

### 5. Создать Personal Access Token (PAT)

1. Зайдите на https://dev.azure.com
2. Создайте организацию (если нет)
3. User Settings → Personal Access Tokens → New Token
4. Name: "VS Code Extensions"
5. Organization: "All accessible organizations"
6. Scopes: **Marketplace → Manage** (полный доступ)
7. Срок действия: выберите подходящий
8. Create и **сохраните токен** (он показывается только один раз!)

### 6. Скомпилировать расширение

```bash
cd /home/shamil/projects/tmetrix/integrations/vscode
npm run compile
```

### 7. Упаковать расширение (опционально, для тестирования)

```bash
vsce package
```

Это создаст файл `tmetrix-0.1.0.vsix` который можно установить локально для тестирования:
```bash
code --install-extension tmetrix-0.1.0.vsix
```

### 8. Опубликовать на Marketplace

```bash
# Войти в систему (используйте PAT токен)
vsce login ВАШЕ_PUBLISHER_ID

# Опубликовать
vsce publish
```

Или сразу с указанием токена:
```bash
vsce publish -p ВАШ_PERSONAL_ACCESS_TOKEN
```

## 📋 Чек-лист перед публикацией

- [ ] Создана иконка icon.png (128x128px)
- [ ] Publisher ID обновлен в package.json
- [ ] Repository URLs обновлены в package.json
- [ ] Установлен vsce: `npm install -g @vscode/vsce`
- [ ] Создан Personal Access Token в Azure DevOps
- [ ] Код скомпилирован: `npm run compile`
- [ ] Расширение протестировано локально
- [ ] Нет ошибок компиляции: `npm run lint`
- [ ] README.md содержит актуальную информацию
- [ ] CHANGELOG.md заполнен

## 🚀 После публикации

1. Расширение появится на https://marketplace.visualstudio.com через несколько минут
2. Проверьте страницу расширения - все ли отображается корректно
3. Установите расширение из маркетплейса для финального теста

## 📝 Обновление версии

При следующих обновлениях:

1. Обновите версию в `package.json` (например, 0.1.0 → 0.1.1)
2. Добавьте изменения в `CHANGELOG.md`
3. Скомпилируйте: `npm run compile`
4. Опубликуйте: `vsce publish`

Или автоматически увеличить версию:
```bash
vsce publish patch  # 0.1.0 → 0.1.1
vsce publish minor  # 0.1.0 → 0.2.0
vsce publish major  # 0.1.0 → 1.0.0
```

## 🔗 Полезные ссылки

- VS Code Marketplace: https://marketplace.visualstudio.com/vscode
- Управление расширениями: https://marketplace.visualstudio.com/manage
- Azure DevOps (для PAT): https://dev.azure.com
- Документация vsce: https://code.visualstudio.com/api/working-with-extensions/publishing-extension

## ⚠️ Важные замечания

1. **Publisher ID** - это ваш уникальный идентификатор. Без него нельзя опубликовать.
2. **Personal Access Token** должен иметь Marketplace → Manage права.
3. **icon.png** должна быть ровно 128x128 пикселей.
4. После публикации изменить Publisher ID нельзя - придется создавать новое расширение.
5. Имя расширения (name в package.json) должно быть уникальным на маркетплейсе.

## 🐛 Возможные проблемы

**Ошибка: "Extension name already exists"**
- Измените поле `"name"` в package.json на уникальное

**Ошибка: "Publisher not found"**
- Сначала создайте Publisher на https://marketplace.visualstudio.com/manage

**Ошибка при компиляции**
- Запустите: `npm install` затем `npm run compile`

---

Удачи с публикацией! 🚀
